/**
 * snippet
 */

const mutationTypes = {
  //INIT: "INIT"
}

// export type State = { initData: Array<any> }

export default {
  
  state () {
    return {
      // initData: []
    }
  },

  getters: {

  },

  actions: {
    // init (({ commit, dispatch, rootState, rootGetters }) {
    //   commit(mutationTypes.INIT, result)
    // }
  },

  mutations: {
    // [mutationTypes.INIT] (state, result) {
    //   state.initData = result; 
    // }
  },

  modules: {

  }

};